{
  "name": "uicogs",
  "version": "0.1.0",
  "private": true,
  "description": "Resource-first schemas and reactive client data workflows.",
  "license": "MIT",
  "author": "UiCogs",
  "type": "module",
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "corepack pnpm -r build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:browser": "corepack pnpm build && playwright test",
    "test:types": "tsd --typings packages/core/src/index.ts --files tests/types/*.test-d.ts",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check:packages": "corepack pnpm -r exec publint --pack=false",
    "check:standalone": "node scripts/check-standalone.mjs",
    "api:report": "corepack pnpm build && node scripts/api-report.mjs --write",
    "api:check": "node scripts/api-report.mjs --check",
    "test:consumers": "node scripts/test-packed-consumers.mjs",
    "test:stress": "vitest run --config vitest.stress.config.ts",
    "budget:record": "corepack pnpm build && node scripts/check-budgets.mjs --write",
    "budget:check": "node scripts/check-budgets.mjs --check",
    "audit:prod": "corepack pnpm audit --prod --audit-level high",
    "sbom:generate": "node scripts/generate-sbom.mjs",
    "check": "corepack pnpm format:check && corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test:coverage && corepack pnpm test:types && corepack pnpm test:browser && corepack pnpm build && corepack pnpm check:standalone && corepack pnpm check:packages",
    "release:check": "corepack pnpm format:check && corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test:coverage && corepack pnpm test:types && corepack pnpm build && corepack pnpm api:check && corepack pnpm budget:check && corepack pnpm test:browser && corepack pnpm test:consumers && corepack pnpm check:standalone && corepack pnpm check:packages && corepack pnpm audit:prod && corepack pnpm sbom:generate"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@eslint/js": "^9.39.2",
    "@playwright/test": "^1.61.1",
    "@quasar/extras": "^2.0.2",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^22.19.1",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@uicogs/core": "workspace:*",
    "@uicogs/http": "workspace:*",
    "@uicogs/quasar": "workspace:*",
    "@uicogs/react": "workspace:*",
    "@uicogs/storage": "workspace:*",
    "@uicogs/vue": "workspace:*",
    "@vitest/coverage-v8": "4.1.10",
    "@vue/test-utils": "^2.4.11",
    "eslint": "^9.39.2",
    "fast-check": "^4.9.0",
    "jsdom": "^29.1.1",
    "prettier": "^3.7.4",
    "publint": "^0.3.17",
    "quasar": "^2.18.6",
    "react": "^19.2.1",
    "react-dom": "^19.2.1",
    "tsd": "^0.33.0",
    "tsup": "^8.5.1",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.53.1",
    "vite": "^8.1.5",
    "vitest": "^4.0.18",
    "vue": "^3.5.26",
    "yaml": "^2.8.2"
  },
  "tsd": {
    "directory": "tests/types"
  }
}
